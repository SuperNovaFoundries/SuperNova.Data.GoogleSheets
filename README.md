GoogleSheets Datasource microservice


For tests to work, you need to place client_id.json file in the test binary output directory. To get client_id.json, you need to generate it via Google Cloud APIs for GoogleSheets by adding
a OAuth credential set.